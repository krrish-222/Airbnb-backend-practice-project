
<style>
/* Nav styles moved inline so this partial is self-contained */
.site-nav{
    display:flex;
    gap:10px;
    align-items:center;
    padding:10px 14px;
    background: rgba(255,255,255,0.06);
    border-radius:10px;
    backdrop-filter: blur(6px);
    -webkit-backdrop-filter: blur(6px);
    margin-right:auto;
}
.site-nav a{
    color: #fff;
    text-decoration:none;
    padding:8px 12px;
    border-radius:8px;
    font-weight:600;
    transition: background .15s ease, transform .12s ease, opacity .12s ease;
    background:transparent;
}
.site-nav a:hover{
    background: rgba(255,255,255,0.08);
    transform: translateY(-2px);
}
.site-nav a.Home{
    background: transparent;
    color: #fff;
    box-shadow: none;
    border: 1px solid rgba(255,255,255,0.04);
}
.site-nav a.Home:hover{opacity:0.95; transform:translateY(-2px)}
.site-nav a.Add-Home{border:1px solid rgba(255,255,255,0.12)}

@media (max-width:600px){
    .site-nav{flex-wrap:wrap; gap:8px; justify-content:center}
    .site-nav a{padding:8px 10px; font-size:0.95rem}
}
</style>

<nav class="site-nav">
    <a class="Home" href="/home">Home</a>
    <a class="Add-Home" href="/add-home">Add Home</a>
    <a class="favourites" href="/favourites">Favourites</a>
    <a class="bookings" href="/bookings">Bookings</a>
    <a class="Host-Homes" href="/host-homes">Hosted Homes</a>
</nav>